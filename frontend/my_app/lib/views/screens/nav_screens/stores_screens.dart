import 'package:flutter/material.dart';

class StoresScreens extends StatelessWidget {
  const StoresScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Stores"),
      ),
    );;
  }
}
