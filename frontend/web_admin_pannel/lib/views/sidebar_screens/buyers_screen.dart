import 'package:flutter/material.dart';

class BuyersScreen extends StatelessWidget {
  const BuyersScreen({super.key});
  static String id = 'buyers-screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("buyers"),
    );
  }
}
