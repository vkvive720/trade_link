import 'package:flutter/material.dart';

class VenderScreen extends StatelessWidget {
  const VenderScreen({super.key});
  static String id = '\vendor-screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("vender"),
    );;
  }
}
